import { Box, Grid } from "@mui/material";
import { TopBar } from "../components";
import styled from "styled-components";
import { useGlobalConfigs } from "../store";

const CartPage = () => {
  return "cart place holder ";
};

export { CartPage };
